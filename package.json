{
  "name": "sentry-render-test",
  "version": "1.0.0",
  "description": "A test of sentry within render.js with contextisolation and nodeintegration turned off",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ."
  },
  "author": "Jimmy Hooker",
  "license": "MIT",
  "devDependencies": {
    "electron": "^14.0.0"
  },
  "dependencies": {
    "@sentry/electron": "^2.5.3"
  }
}
